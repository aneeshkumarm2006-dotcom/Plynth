# Work Log — 2026-06-12

Everything built today, with **test points** for each item so they can be folded
into `test.md`. Ordered by commit. All work is committed; both apps type-check +
build clean; unit suite is green (**270 tests**).

Legend: ☐ test point · ⚠️ regression watch

---

## 1. Test suite expansion + close-speed parser fix
**Commit:** `35d5d21` · 94 → 252 unit tests.

What changed:
- Added 158 edge-case unit tests across utils, broker+lender presenters, services (mock mode), criteria.
- **Bug fixed:** `builderToRow` close-speed parser only split on en-dash (`–`) and treated a legit `0` as missing. Now matches en-dash OR hyphen and honours `0` bounds (`packages/supabase/src/services/criteria.ts`).

Test points:
- ☐ `[unit]` close-speed parsing: "7–10 days", "7-10 days", "5-30 days" → {5,30}, "0–0 days" → {0,0}, garbage → {7,10}.
- ⚠️ Criteria round-trip (`builderToRow`/`rowToBuilder`) still stable — `rowToBuilder` emits en-dash, parser accepts it.

---

## 2. Draft + edit-deal flows; realtime filter
**Commit:** `6ea20ca` — previously no-op buttons, now functional.

What changed:
- **Save draft:** `dealsService.create(brokerId, input, status)` ('active' | 'draft'). Drafts get no `submitted_at` and aren't matched.
- Broker **Pipeline** draft rows show a **Submit** action; `dealsService.submitDraft(id)` promotes draft → active + stamps `submitted_at`.
- Broker **DealDetail** shows **"Submit to marketplace"** for drafts.
- **Edit deal:** real inline `EditDealPanel` (loan amount, term, notes) via `dealsService.update` (now graceful in mock mode); validates ≥ $50k.
- **Realtime filter:** lender Dashboard/Matched only refetch on `new_match` notifications (not every insert).

Test points:
- ☐ `[int]` Save draft → deal `status='draft'`, `submitted_at=null`, not in lender matches.
- ☐ `[int]` Draft appears under Pipeline "Draft" filter with Submit action; submitting flips to `active`, stamps `submitted_at`, starts matching.
- ☐ `[int]` Edit deal saves loan amount/term/notes; `updated_at` changes; loan < $50k rejected (client + DB `valid_loan_amount`).
- ☐ `[int]` Realtime: an `offer_received`/`deal_funded` insert does NOT refetch the matched list; a `new_match` does.
- ☐ `[unit]` `dealsService.create` status active/draft; `update` echoes patch in mock; `submitDraft` resolves (covered).

---

## 3. Tier 0 — capture full deal fields + real facts/why-matched + view tracking
**Commit:** `30e7189` — finished "half-built" wiring on already-existing backend.

What changed:
- **Submit now persists** `beacon_score`, `exclusion_flags` (new **Property flags** chips), `rate_min/max`, `requested_rate_range` — these were collected then dropped, which **degraded match scoring** for every UI-submitted deal.
- **BUG FIXED (submit-blocking):** Submit sent `property_type: 'Detached'` into an enum (`residential|commercial|land|multi-residential`) → a live insert would have been **rejected**. Now mapped via `PROPERTY_TYPE_MAP`; parsed province guarded against its enum too.
- **Real deal facts** on both DealDetails (property type, appraised value, beacon band, rate expectation) instead of hardcoded `$590,000` / `680–720`.
- `matchedService` selects `estimated_value_cents`, `beacon_score`, `property_type`, `is_self_employed`.
- **Real "Why this matched"** (lender): computed pass/fail per factor (asset, geography, LTV vs their limit, loan size, beacon, BFS) from the deal vs the lender's criteria — was hardcoded strings.
- **View tracking:** lender DealDetail calls `recordView` on mount → increments `lender_deal_interactions.views_count` (existing RPC; was dead code).

Test points:
- ⚠️ `[int]` **Submit a real deal** (write-path never tested live): confirm insert succeeds with `property_type='residential'`, and `beacon_score`/`exclusion_flags`/`rate_min/max`/`requested_rate_range`/`estimated_value_cents` all persist.
- ☐ `[int]` Property flags chips persist into `exclusion_flags`; they affect lender matching (deal flagged "Rural" excluded by a lender excluding "Rural").
- ☐ `[int]` Deal facts on both detail pages reflect the actual row, not the old literals.
- ☐ `[int]` Lender "Why this matched" ✓/✕ are correct vs that lender's criteria (test a deal that fails one factor — e.g. LTV over limit shows ✕).
- ☐ `[int]` Opening a deal as a lender increments `views_count`.
- ☐ `[unit]` `beaconBand` thresholds, `titleCase` (covered). ⚠️ Add `parseRateRange` + `PROPERTY_TYPE_MAP` tests (currently un-exported in `Submit.tsx`).

---

## 4. test.md — master test plan
**Commit:** `8cc2dcb` — the end-of-project testing brief (keep it in sync).

Test points: n/a (this is the plan itself).

---

## 5. Offer comparison — effective annual cost (APR-normalized)
**Commit:** `4c1f3f2` — broker decision-quality feature.

What changed:
- New `effectiveAnnualCost(rate, lenderFee, brokerFee, termMonths)` util = rate + fees annualized over term.
- Broker DealDetail offer cards show **"Effective annual cost"** and flag the **"Lowest true cost"** offer (which can differ from the lowest headline rate).
- `OfferDisplay` exposes numeric `lenderFeeValue`/`brokerFeeValue`/`termMonthsValue`.

Test points:
- ☐ `[unit]` `effectiveAnnualCost`: (9.25,2,1,12)=12.25; (9.25,2,1,24)=10.75; lower-rate-higher-fee can exceed higher-rate-lower-fee; term 0 doesn't divide-by-zero; defaults (covered).
- ☐ `[manual]` On a deal with ≥2 offers, the "Lowest true cost" badge lands on the lowest effective-cost offer, not necessarily the lowest rate.
- ☐ `[manual]` With one offer, no badge shown.

---

## 6. Matched feed — Province / Loan-size / Match-score filters
**Commit:** `9c3a9a8` — the brief asked for these; only asset-class existed.

What changed:
- Added Province dropdown (derived from provinces present in the feed), Loan-size buckets (<$500K / $500K–$1M / $1M–$2M / >$2M), Min-match-score (70+/80+/90+), plus a "N of M" result count.
- Logic extracted to pure `filterAndSortMatched(rows, filters)` in `apps/lender/src/lib/present.ts`.

Test points:
- ☐ `[unit]` `filterAndSortMatched`: each filter alone, combined intersection, newest-sort by `matched_at` desc, no input mutation (covered, 9 tests).
- ☐ `[manual]` Province dropdown only lists provinces present; result count updates; empty state when over-filtered.
- ⚠️ "Expiring soon" sort is still a **no-op** (no expiry field on MatchedDeal) — confirm inert; scope a fix later.

---

## 7. Admin portal (new third app)
**Commits:** `Admin portal …` + `Admin UI polish …` — built with architect + database-specialist + frontend-engineer + design-eng agents.

What changed:
- New `apps/admin` (role-gated) to monitor users + marketplace activity: Overview (metrics + editorial bar charts), Users (verify/reject), Activity (live audit feed), Deals + Offers monitors.
- `adminService` (+ `ADMIN_MOCK`); `'admin'` added to `UserRole`.
- DB migrations **written but NOT yet applied**: `0007` adds the `admin` role; `0008` adds the self-escalation guard, additive admin read-all RLS (reusing `current_user_role()`), admin RPCs, `session.login` trigger.
- Apply scripts: `scripts/apply-admin-migrations.mjs`, `scripts/seed-admin.mjs`.

Test points: see `test.md` §9b (security gates, RPC/metrics correctness, the no-txn apply caveat, PIPEDA PII).
⚠️ **Live DB not yet changed** — apply + seed pending user approval; admin portal currently runs in mock mode only.

---

## Cross-cutting notes for testing
- ⚠️ **Write-path has never been run live** — items in §2/§3 marked `[int]` are the highest-value untested paths (submit, draft, edit, the `property_type` enum fix, view tracking). A single live "submit a deal as broker@plynth.test" exercises most of it.
- All new pure logic is unit-tested; the gaps (`parseRateRange`, `PROPERTY_TYPE_MAP`, lender `whyMatched`) need a small extract-to-util before they're testable — noted in `test.md` §1.
- Nothing today touched the deferred **DB pass** (RLS isolation, offer-expiry cron, broker demand signal, borrower-reveal enforcement) or external-key features (Stripe, FSRA, uploads).

---

_Generated 2026-06-12. Source of truth for today's changes; fold the ☐ points into `test.md`._
